# --- 数据集 (绝对不能传) ---
datasets/
*.zip
*.tar.gz

# --- Python 编译缓存 (垃圾文件) ---
__pycache__/
*.pyc
*.pyo

# --- Mac 系统生成的文件 ---
.DS_Store

# --- VS Code 配置 (可选，通常不传) ---
.vscode/

# --- 环境变量 (防止密钥泄露) ---
.env

# --- 权重文件 (checkpoint，上传时忽略) ---
weights/**/*.pt
weights/
*.pt